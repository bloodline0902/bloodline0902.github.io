# pnpm 10 no longer hoists anything publicly by default. eslint-config-next
# pulls in its plugins as its own dependencies, but ESLint resolves plugins
# from the project root, so they must be hoisted or `pnpm lint` cannot start.
public-hoist-pattern[]=*eslint*
